import React from 'react';
import {Button} from '@storybook/react/demo';

class TestButton extends React.Component {
  render() {
    return (
      <Button>
        <span>Testing a simple component in Storybook 5</span>
      </Button>
    );
  }
}

export default TestButton;
